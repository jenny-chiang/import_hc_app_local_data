name: 'Create Release'

on:
  push:
    branches:
      - main
    tags:
      - '*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: 'Create Release'
        uses: softprops/action-gh-release@v24
        with:
          generate_release_notes: true
